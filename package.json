{
  "name": "cloudflare-pages-exploit",
  "version": "1.0.0",
  "description": "Cross-tenant exploit for Cloudflare Pages",
  "main": "exploit.js",
  "scripts": {
    "install": "node-gyp rebuild",
    "start": "node exploit.js",
    "build": "node-gyp configure build"
    "exploit": "mkdir -p dist && npx node-gyp rebuild && node exploit.js"
  },
  "dependencies": {
    "node-gyp": "^11.5.0"
  },
  "devDependencies": {
    "node-addon-api": "^8.7.0"
  }
}
